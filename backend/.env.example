# ---- Server ----
PORT=5000
NODE_ENV=production
CLIENT_URL=https://placementprep-wheat.vercel.app

# ---- Database ----
# Get a free cluster at https://www.mongodb.com/atlas
MONGO_URI=mongodb+srv://abhinay:Abhi2005@cluster0.l37knjz.mongodb.net/placement-platform

# ---- Auth ----
JWT_SECRET=abhinayvaram205@vitap
JWT_EXPIRES_IN=7d
OTP_SECRET=ironmanspidermanthorhulkmaxverstappen
OTP_EXPIRY_MINUTES=10

# ---- Email (Nodemailer) ----
# Works with Gmail (use an App Password, not your real password),
# or any SMTP provider (Brevo, Mailtrap, SendGrid SMTP, etc.)

SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=b16e94001@smtp-brevo.com
SMTP_PASS=bskwlww349WtHeF
EMAIL_FROM="placement prep<abhinayvarma11.6@gmail.com>"


# If SMTP vars are left blank, OTPs are printed to the server console
# instead of emailed — handy for local dev without email credentials.
