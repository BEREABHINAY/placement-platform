# Only needed in production. Leave unset for local dev — the Vite proxy in
# vite.config.js handles routing /api calls to your local backend automatically.
#
# Set this in your hosting provider's environment variables to your deployed
# backend's URL, including the /api path:
VITE_API_URL=https://your-backend.onrender.com/api
